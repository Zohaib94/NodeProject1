const routes = {
  index_route: '/',
  about_route: '/about',
  records_index_route: '/records',
  show_record_route: '/records/:id',
  new_record_route: '/records/new',
  create_record_route: '/records',
  edit_record_route: '/records/:id/edit',
  update_record_route: '/records/:id',
  destroy_record_route: '/records/:id',
};

module.exports = routes;
